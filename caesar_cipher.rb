# frozen_string_literal: true

alphabet_array = ('a'..'z').to_a * 1000

puts 'Enter your secret message:'
secret_message = gets.chomp

puts 'Enter the key:'
key = gets.chomp.to_i

def caesar_cipher(msg, shift)
  
end

caesar_cipher(secret_message, key)
